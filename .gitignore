# macOS
.DS_Store

# Editors
.idea/
*.swp

# Node (not shipped, but keep stray installs out)
node_modules/
npm-debug.log*

# Local secrets / env
.env
.env.*

# Local dev tooling / machine-specific config (never publish)
.gstack/
.claude/
.claude/settings.local.json
